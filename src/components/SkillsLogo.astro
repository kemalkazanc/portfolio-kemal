<div class="skillsLogo"><slot /></div>

<style>
  .skillsLogo {
    display: inline-flex;
    align-items: center;
    padding: 0.5rem 1rem;
    gap: 0.5rem;
    border: 1px solid var(--accent-regular);
    background-color: transparent; /* Pas de couleur de fond */
    border-radius: 999rem;
    font-size: var(--text-md);
    line-height: 1.35;
    white-space: nowrap;
  }
  .skillsLogo svg {
    width: 1.5em; /* Assurez-vous que toutes les icônes ont la même taille */
    height: 1.5em;
  }
</style>
